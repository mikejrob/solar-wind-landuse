# Only scratch is ignored — primary sources (data/raw), GIS layers (data/gis),
# and the curated archive (sources/) are all tracked so the public repo is
# self-contained. Largest file is a 59 MB DEM tile (under GitHub's 100 MB limit).
# If the ~1 GB weight becomes a problem, migrate big binaries to Git LFS or a
# Zenodo deposit (see README).
cache/
.DS_Store
__pycache__/
*.pyc
.venv/
